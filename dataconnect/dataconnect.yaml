specVersion: "v1"
serviceId: "policiaalerta"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "policiaalerta-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
